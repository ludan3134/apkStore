import {Channel} from "../../../../../api/ks/v1/km_pb";

export type ThirdCalssStore = {
    ThirdCalssEdit: Channel;
    ThirdCalsslist: Channel[];
    ThirdCalssFilter: Channel;
    ThirdCalsstableUrl: string;
};
